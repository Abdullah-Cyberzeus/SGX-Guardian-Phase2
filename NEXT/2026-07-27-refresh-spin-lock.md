# Refresh the multipart spin dependency

- Update the lockfile from yanked `spin` 0.9.8 to the compatible, non-yanked 0.9.9 release.
- Keep the existing `multer` and `axum` dependency constraints and security policy unchanged.
