# Fix pinned protoc setup

- Install protoc 35.1 from the official protobuf release with a repository-owned installer and a hard-coded SHA-256 checksum, avoiding unsupported tool manifests and deprecated action runtimes.
